expect = 10

lives = 5

console.log(`Total Life : ${lives}`)

while(true){

    if(lives <= 0)  {
        console.log("Limit Exceeded");
        break;
    }
    else {
        guess = Nunmber(propmt(guess a number))
    }
}
